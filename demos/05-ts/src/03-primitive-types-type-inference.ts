// type inference. If we give an initial value,the type is inferred from the value.
let quantity = 100;

let message: string;
message = "Hello, world";
// message = 100; // error

let isWinter = false;

let x = null;

export {};
