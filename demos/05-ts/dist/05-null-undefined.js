"use strict";
// `null` data type can have only null as its value
// `undefined` data type can have only undefined as its value
Object.defineProperty(exports, "__esModule", { value: true });
// On its own `null`, '`undefined` data types are not useful. They will be useful when we combne with other data types (using union)
let x;
x = null;
